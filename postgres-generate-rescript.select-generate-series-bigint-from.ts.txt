import type pg from 'pg';

export type SelectGenerateSeriesBigintFromResult = {
	g: bigint | null;
}

export async function selectGenerateSeriesBigintFrom(client: pg.Client | pg.Pool | pg.PoolClient): Promise<SelectGenerateSeriesBigintFromResult[]> {
	const sql = `
	SELECT * FROM generate_series(1::bigint, 5::bigint) AS g
	`
	return client.query({ text: sql, rowMode: 'array' })
		.then(res => res.rows.map(row => mapArrayToSelectGenerateSeriesBigintFromResult(row)));
}

function mapArrayToSelectGenerateSeriesBigintFromResult(data: any) {
	const result: SelectGenerateSeriesBigintFromResult = {
		g: data[0]
	}
	return result;
}